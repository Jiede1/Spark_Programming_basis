* scala数据类型：Byte Short int Long Float Double Char String Boolen Unit(表示void) Null Any 。。。  
* 在 Scala 中，使用关键词 "var" 声明变量，使用关键词 "val" 声明常量。  
* Scala拥有数据类型自动推断特性  
* Scala 拥有作用域保护 private[x] 这里的x指代某个所属的包、类或单例对象。如果写成private[x],读作"这个成员除了对[…]中的类或[…]中的包中的类及它们的伴生对像可见外，对其它所有类都是private  
* Scala运算符  
* Scala if -else  
* Scala for循环 for(i <- 1 to 10)   
  
* Scala方法和函数: val 定义函数 def 定义方法
方法定义：  
def function(参数列表):[return type] = {  
	function body  
	return [expr]  
}  
1. 
2.可变参数 args:String*   
3.默认参数  
4.函数嵌套  
5.
